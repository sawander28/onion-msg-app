
plugins {
    id 'com.android.application' version '8.11.0' apply false
    id 'com.android.library' version '8.11.0' apply false
    id 'org.jetbrains.kotlin.android' version '2.2.0' apply false
}

task clean(type: Delete) {
    delete rootProject.buildDir
}

ext {
    versionName = "0.1.0"
    LIBRARY_GROUP = "wandsas"
    LIBRARY_ARTIFACT_ID = "arti.tor"
    LIBRARY_VERSION_NAME = versionName
    LIBRARY_GIT_URL = "git@rpi3.lan:arti-messenger.git"
    OSS_SIGNING_KEY_ID = "sascha.wander@onionmail.com"
    OSS_SIGNING_KEY = "0x56303570E0D0B038"
}

